Material = {
	blockColor = nil, -- [read-only] Color
	color = nil, -- [read-only] Color
	costFactor = nil, -- [read-only] double
	density = nil, -- [read-only] double
	name = nil, -- [read-only] string
	strengthFactor = nil, -- [read-only] double
	value = nil -- int
}

-- @return A new instance of Material
function Material(type)
end

function Material.__eq(other)
end

function Material.__lt(other)
end

function Material.__tostring()
end

