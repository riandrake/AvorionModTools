Ray = {
	direction = nil, -- vec3
	origin = nil, -- vec3
	planeIntersectionThickness = nil -- float
}

-- @return A new instance of Ray
function Ray(pos, size)
end

function Ray.distanceToPoint(point)
end

function Ray.distanceToPoint2(point)
end

-- @return nothing
function Ray.findClosestPoints(other, a, b)
end

